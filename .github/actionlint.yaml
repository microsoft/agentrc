# Ignore auto-generated workflow files that cannot be edited directly.
# agentics-maintenance.yml is generated by `gh aw compile` and uses an empty
# string as a choice option (a valid GitHub Actions pattern that actionlint
# rejects with [syntax-check]).
paths-ignore:
  - ".github/workflows/agentics-maintenance.yml"
